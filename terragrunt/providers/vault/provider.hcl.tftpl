variable "vault_address" {
  type    = string
  default = null
}

variable "vault_token" {
  type      = string
  sensitive = true
}

provider "vault" {
  address = var.vault_address
  token   = var.vault_token
}
